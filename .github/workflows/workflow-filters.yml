name: Event Filtering
on:
    push:
      branches-ignore:
        - feature/*
        - test/**
      branches:
        - main
        - '!feature/*'
    
    pull_request:
        types:
          - opened
          - closed
        paths-ignore:
          - README.md
        branches:
          - main
        branches-ignore:
          - feature/*



jobs:
    job1:
      runs-on: ubuntu-latest
      steps:
      - run: echo "Ola"