name: Event Filtering
on:
    push:
      branches:
        - main
      branches-ignore:
        - feature/*

    pull_request:
        types:
          - opened
          - closed
        paths-ignore:
          - README.md



jobs:
    job1:
      runs-on: ubuntu-latest
      steps:
      - run: echo "Ola"