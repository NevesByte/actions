name: Reusable Workflow Use
on:
    push:

jobs:
    job-caller: 
        uses: ./.github/workflows/reusable-workflow.yml
        with:
            enviroment: production
        secrets:
            PASSWORD: secrets.PASSWORD
    test:
        runs-on: ubuntu-latest
        steps:
          - run: echo "${{ needs.deploy.outputs.application-url }}"
        